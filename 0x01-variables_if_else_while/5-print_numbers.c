#include <stdio.h>
/**
 * main - starting point execution.
 * Description: Print all numbers of base 10, starting from 0.
 * Return: always 0 (success)
 */
int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
	{
		printf("%i\n", i);
	}
	return (0);
}
